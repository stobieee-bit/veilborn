{
  "name": "veilborn",
  "version": "0.1.0",
  "private": true,
  "description": "VEILBORN — First-Person Sci-Fi Survival. Phase 1 playable prototype (Three.js + TypeScript).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
